# CPU-only torch. Install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# The CUDA build is ~2.5 GB larger and is pointless unless the pod has a GPU.
torch==2.5.1
torchvision==0.20.1
easyocr==1.7.2
opencv-python-headless==4.10.0.84   # headless: the GUI build needs libGL, absent in ubi minimal
pillow==11.0.0
numpy==2.1.3
# Apache/BSD-licensed PDF rasteriser. Deliberately not PyMuPDF, which is AGPL.
pypdfium2==4.30.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
